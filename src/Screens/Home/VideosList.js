import React from 'react';
import { View } from 'react-native';

const VideosList = () => {
    return (
        <View>
            <Text>VideosList</Text>
        </View>
    );
};

export default VideosList;
