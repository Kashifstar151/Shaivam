import { Text, View } from 'react-native';

export const RenderCardForAddTemple = ({ item }) => {
    return (
        <View>
            <Text>RenderCardForAddTemple {item}</Text>
        </View>
    );
};
